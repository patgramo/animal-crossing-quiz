import React, { useState, useEffect } from "react";

export default function getVillagerData() {
  // const [villagerData, setVillagerData] = useState([]);
  //this function gets data based on month. I want to pass it props and each prop is a different month
  //this map function is making a new array of objects with the data I need and excluding extra data
  //   setVillagerData(
  //     data
  //       .map((obj) => ({
  //         name: obj.name["name-USen"],
  //         personality: obj.personality,
  //         birthday: obj["birthday-string"],
  //         catchphrase: obj["catch-phrase"],
  //         image: obj["image_uri"]
  //       }))
  //       .filter((object, props) => {
  //         return object.personality === "Cranky";
  //       })
  //   );
  // };
  // useEffect(() => {
  //   fetchData();
  // }, []);
}
